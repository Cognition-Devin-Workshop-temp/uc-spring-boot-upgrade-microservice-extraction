package io.spring.api;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class ArticleStatisticsApi {
  // TDD: Empty controller — tests should fail until endpoints are implemented.
}
